1. Install Qt 5
2. qmake && make
(application is built with -g -pg -fno-omit-frame-pointer)
3. uftrace  --nest-libcall --disable -T MainWindow::checkboxChecked@trace_on  ./test-checkbox
4. Click on checkbox to start tracing
